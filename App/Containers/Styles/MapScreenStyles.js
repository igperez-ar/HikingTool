import { StyleSheet } from 'react-native'
import { Metrics } from '../../../ignite/DevScreens/DevTheme';

export default StyleSheet.create({
    map: {
      width: Metrics.screenWidth,
      height: 450
    }
  })
